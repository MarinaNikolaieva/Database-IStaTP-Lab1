@model Lab1.PizzeriaMeal

@{
    ViewData["Title"] = "Create";
}

<h1>Create @ViewBag.ErrorMessage</h1>

<h4>PizzeriaMeal</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="MealId" class="control-label"></label>
                <select asp-for="MealId" class="form-control" asp-items="ViewBag.MealId"></select>
            </div>
            <div class="form-group">
                <label asp-for="PizzeriaId" class="control-label"></label>
                <select asp-for="PizzeriaId" class="form-control" asp-items="ViewBag.PizzeriaId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
