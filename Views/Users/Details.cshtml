@model Lab1.Users

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>
<p>
    @Html.ActionLink("View all reviews by this user", "Reviews", new { id = ViewBag.UserId })
</p>

<div>
    <h4>Users</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.UserName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.UserName)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
