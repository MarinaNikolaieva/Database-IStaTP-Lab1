@model Lab1.Animatronic

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>
<p>
    @Html.ActionLink("Move animatronic to stock", "setPizzeriaNull", new { id = ViewBag.Id })
</p>

<h4>Animatronic</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="PizzeriaId" class="control-label"></label>
                <select asp-for="PizzeriaId" class="form-control" asp-items="ViewBag.PizzeriaId"></select>
                <span asp-validation-for="PizzeriaId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SpeciesId" class="control-label"></label>
                <select asp-for="SpeciesId" class="form-control" asp-items="ViewBag.SpeciesId"></select>
                <span asp-validation-for="SpeciesId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AnimatronicName" class="control-label"></label>
                <input asp-for="AnimatronicName" class="form-control" />
                <span asp-validation-for="AnimatronicName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AnimatronicInfo" class="control-label"></label>
                <input asp-for="AnimatronicInfo" class="form-control" />
                <span asp-validation-for="AnimatronicInfo" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
